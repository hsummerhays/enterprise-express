1. CLAUDE.md file — Claude reads your full project context from session zero. No more re-explaining your stack every time.
2. Custom slash commands — Turn 200-word prompts into /add-skill or /review-pr. Two keystrokes, full execution.
3. Git hooks — Claude reviews every file you write automatically. Bugs die before they reach your repo.
4. MCP servers — Connect Claude to your live database, Notion, or Airtable. No more copy-pasting context.
5. Headless mode — Run autonomous Claude loops overnight. Wake up to shipped features.
6. Sub-agents — Spawn parallel Claudes. One researches, one codes, one tests — simultaneously.
7. Context compaction — Never lose the thread mid-project. Checkpoint strategy that works across 100k+ token tasks.